{
  "connector_key": "stibee",
  "version": "1.0.0",
  "channel": "stibee",
  "fields": [
    {
      "name": "create.subject",
      "type": "string",
      "source": "content",
      "required": true
    },
    {
      "name": "html",
      "type": "string",
      "source": "content",
      "required": true
    },
    {
      "name": "create.senderEmail",
      "type": "string",
      "source": "org_config",
      "required": true,
      "setup_hint": "조직의 발신 이메일 — Stibee 워크스페이스 설정에서 확인 후 조직 설정 화면에 등록"
    },
    {
      "name": "create.senderName",
      "type": "string",
      "source": "org_config",
      "required": true,
      "setup_hint": "조직의 발신자명 — 조직 설정 화면에 등록"
    },
    {
      "name": "create.listId",
      "type": "number",
      "source": "org_config",
      "required": true,
      "setup_hint": "Stibee 워크스페이스의 리스트 ID — 워크스페이스 설정 > 주소록에서 확인 후 조직 설정 화면에 등록"
    },
    {
      "name": "create.groupIds",
      "type": "array",
      "source": "org_config",
      "required": false,
      "constraints": {
        "itemType": "number"
      },
      "setup_hint": "선택 — Stibee 워크스페이스의 그룹 ID"
    },
    {
      "name": "create.segmentIds",
      "type": "array",
      "source": "org_config",
      "required": false,
      "constraints": {
        "itemType": "number"
      },
      "setup_hint": "선택 — Stibee 워크스페이스의 세그먼트 ID"
    }
  ],
  "requires_env": [
    "STIBEE_ACCESS_TOKEN"
  ]
}
